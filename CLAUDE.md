@AGENTS.md

Read [CONTEXT.md](CONTEXT.md) for the PC/Bedrock dispatch model and [SECURITY.md](SECURITY.md) before parsing external chunk bytes. Keep src/index.js, implementation modules, test fixtures, and types/index.d.ts synchronized.

A new version entry is valid only when its palette, section, height, biome, light, and wire semantics match the registry data. Node.js 14 or newer is the package declaration; do not raise or lower that claim without changing package.json and CI.
